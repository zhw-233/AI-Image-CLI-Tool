# 安全说明

## API Key

不要提交真实 API key 或服务商凭证。

下面这些本地文件会被有意忽略：

- `.env`
- `.env.active`
- `providers.json`
- `outputs/` 目录中的生成图片

如果 key 被误提交或泄露，请立刻去服务商后台撤销旧 key，并重新创建新的 key。

## 服务商行为

不同图片 API 服务商的计费和超时行为可能不完全一致。有些服务商即使最终客户端收到的是 `524` 这类超时错误，也可能已经对该请求计费。
